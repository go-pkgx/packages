# pkgx pantry projects to build, one per line (blank lines / # comments ignored).
# The pantry is built progressively: start with dependency-free leaves that build
# cleanly, then grow this list outward toward the full pantry.
#
# Wave 1 — leaves (no build dependencies).
lz4.org
